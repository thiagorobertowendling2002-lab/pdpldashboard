.streamlit/secrets.toml
__pycache__/
*.pyc
.venv/
venv/

# Dados confidenciais: so vao pro git porque o repositorio e privado.
# Se o repo voltar a ficar publico por qualquer motivo, re-adicionar "data/" aqui
# ANTES de fazer qualquer commit.
